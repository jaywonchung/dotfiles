#!/bin/zsh
# NOTE: path linked to ~/.xmonad/xmonad.hs

export DISPLAY=":0"

BGDIR="$HOME/Wallpapers"
RANDOM=$$$(date +%s)
BGLIST=("$BGDIR"/*.jpg)
BGNUM=$(ls -1 "$BGDIR" | wc -l)
NEXTONE=$(( $RANDOM % $BGNUM ))
cp "$BGLIST[$NEXTONE]" "$BGDIR"/theone.jpg
nitrogen --restore > /dev/null 2>&1
